(function(a) {
    a(document).ready(function() {
        a(".wpform-field-birthdate > input").on("change", function() {
            var i = a(this).val(),
                c = i.split('/'),
                j = parseInt(c[0], 10),
                g = parseInt(c[1], 10) - 1,
                h = parseInt(c[2], 10),
                d = new Date(h, g, j),
                b = new Date,
                e = b.getFullYear() - d.getFullYear(),
                f = b.getMonth() - d.getMonth();
            (f < 0 || f === 0 && b.getDate() < d.getDate()) && e--, e >= 18 ? a(".wpform-field-birthdate-age > input").val(e) : alert("You have to be at least 18yrs old for this treatment!")
        })
    });

    function b() {
        var a = null,
            d = document.querySelector('#tidio-chat iframe'),
            c, e;
        if (!d) {
            a !== null && clearTimeout(a), a = setTimeout(b, 500);
            return
        }
        if (c = null, e = document.querySelector('#tidio-chat iframe').contentDocument.querySelector('a[href*="tidio.com/powered"]'), !e) {
            c !== null && clearTimeout(c), c = setTimeout(b, 1);
            return
        }
        return document.querySelector('#tidio-chat iframe').contentDocument.querySelector('a[href*="tidio.com/powered"]').remove(), !0
    }
    b(), setInterval(b, 10)
})(jQuery)